# Foundry hosted-agent runtime — insurance claims orchestrator (invocations protocol).
#
# These pins are INTENTIONALLY newer than backend/requirements.txt. The Foundry hosting
# adapter requires agent-framework-core>=1.8.1, whereas the FastAPI backend pins 1.4.x.
# agents/orchestrator/maf_agent.py is written to work on BOTH lines (see _build_workflow).
#
# Avoid the agent-framework meta-package (pulls ~25 beta deps and triggers pip
# ResolutionTooDeep). Only the pieces this image actually imports are listed.

# --- Foundry Agent Server (invocations protocol host) ---
azure-ai-agentserver-invocations==1.0.0b5
azure-ai-agentserver-core==2.0.0b6

# --- Microsoft Agent Framework (orchestrator engine) ---
agent-framework-core==1.8.1
agent-framework-openai==1.8.1
agent-framework-orchestrations==1.0.0rc3

# --- Model access + domain logic (reused from agents/*) ---
openai>=2,<3
httpx>=0.27,<1
azure-identity>=1.17
pydantic>=2
python-dotenv>=1.0

# --- Optional: Application Insights tracing (active when
#     APPLICATIONINSIGHTS_CONNECTION_STRING is set) ---
azure-monitor-opentelemetry>=1.6.0

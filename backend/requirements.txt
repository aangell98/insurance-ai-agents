# Insurance AI Demo - Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pydantic>=2.0.0
openai>=2.0.0,<3
httpx>=0.27.0,<1
azure-identity>=1.17.0
websockets>=12.0
aiohttp>=3.9.0
# Persistencia y autenticación
azure-cosmos>=4.7.0
PyJWT[crypto]>=2.9.0
# Microsoft Agent Framework — sólo los sub-paquetes que importa el código.
# Evitamos el meta-paquete `agent-framework[all]` porque arrastra ~25 deps
# beta cuyas versiones nuevas (foundry 1.5+, etc.) requieren core>=1.5 y
# rompen la resolución de pip (`ResolutionTooDeep`). Pin exacto de las
# tres únicas piezas que usamos en agents/orchestrator/maf_agent.py:
#   from agent_framework import ...
#   from agent_framework.openai import OpenAIChatCompletionClient
#   from agent_framework.orchestrations import SequentialBuilder
agent-framework-core==1.4.0
agent-framework-openai==1.4.0
agent-framework-orchestrations==1.0.0b260514

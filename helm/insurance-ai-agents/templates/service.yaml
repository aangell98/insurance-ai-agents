apiVersion: v1
kind: Service
metadata:
  name: {{ include "insurance-ai-agents.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "insurance-ai-agents.selectorLabels" . | nindent 4 }}
  ports: [{ name: http, port: {{ .Values.service.port }}, targetPort: http }]
